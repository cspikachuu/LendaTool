/** @type {import('ts-jest/dist/types').InitialOptionsTsJest} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // clearMocks: true,
  // moduleFileExtensions: ['js', 'jsx', 'ts', 'json', 'tsx', 'node'],
  // roots: ['<rootDir>/client', '<rootDir>/server'],
  // testMatch: ['**/__tests_/**/*.[jt]s?(x)', '**/?(*.)+(spec|test).[tj]s?(x)'],
  // transform: {
  //   '^.+\\.(ts|tsx)$': 'jest'
  // }
};